// import { useEffect, useState } from "react";
// import { CartState, ProductToCartCred } from "../types/cartTypes";
// import { useNavigate } from "react-router";
// import { useAppDispatch } from '../hooks';
// import { useSelector } from "react-redux";

export const AddProduct: React.FC = () =>{
    // const [credentials, setCredentials] = useState<ProductToCartCred>({ product: [] });
    // const dispatch = useAppDispatch();
    // const navigate = useNavigate();
    // const isProductAdded = useSelector((cartState: CartState) => cartState.loading);

    // useEffect(() => {
    //     if (isProductAdded) {
    //         navigate('/');
    //     }
    // }, [isProductAdded, navigate]);

    // const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    //     setCredentials({ ...credentials, [e.target.name]: e.target.value });
    //   };

    return (
        <div>
            
        </div>
      );
}
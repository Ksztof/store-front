import LoginForm from "../components/LoginForm";

export const Login = () => {
    return (
        <div>
            <p> Log in for full experience </p>
            <LoginForm />
        </div>
    );
}